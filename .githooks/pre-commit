#!/bin/sh
# ??????????????? dsh web ??????????????
root=$(git rev-parse --show-toplevel)
powershell.exe -NoProfile -ExecutionPolicy RemoteSigned -File "$root/tools/check-syntax.ps1"
if [ $? -ne 0 ]; then
  echo ""
  echo "== ??????????? =="
  exit 1
fi
